# Настройки сайта

Все настройки — **Настройки сайта** (`?tab=config_vars`).  
Секции сворачиваются в **аккордеон**.

---

## 📞 Контактная информация

| Поле | HTML | Валидация |
|------|------|-----------|
| **Название компании** | ✅ | Санитайзер |
| **Слоган** | ✅ | Санитайзер |
| **Телефон** | ❌ | Только `0-9+` |
| **Email** | ❌ | `FILTER_VALIDATE_EMAIL` |
| **Адрес** | ❌ | `e()` при выводе |

**HTML разрешён** — можно `<span>`, `<b>`, `<br>`, ссылки.  
**HTML запрещён** — санитайзер уберёт при попытке вставить.

**Пример** **названия**:
```html
AlPin<span class="text-[var(--primary-color)]">CMS</span>
```
**Результат** — «**AlPin**» **чёрным**, «**CMS**» **основным** **цветом**.

---

## 🖼 Визуальные элементы

| Поле | Рекомендуемый размер |
|------|---------------------|
| **Логотип** | 200×60px (PNG, SVG) |
| **Favicon** | 32×32 или 64×64 (PNG, ICO, SVG) |

**Загрузка:** кнопка **«Фото»** → медиа-модалка → выбор из `images/` или загрузка.

**Путь:** сохраняется **относительный** (`images/logo.png`).

---

## 🎨 Цветовая схема

**8 цветов:**

| Переменная | Где используется |
|-----------|------------------|
| `primary_color` | Кнопки, ссылки, акценты |
| `primary_dark` | Hover кнопок |
| `bg_main` | Фон страницы |
| `bg_card` | Фон карточек |
| `bg_section` | Фон секций |
| `text_main` | Основной текст |
| `text_muted` | Второстепенный текст |
| `border_color` | Границы |

**Формат:** `#RRGGBB`.  
**Синхронизация:** color-picker ↔ text-input.

**В** **CSS** **используются** **как** **CSS-переменные:**
```css
:root {
    --primary-color: #10B981;
    --primary-dark: #059669;
    ...
}
```

---

## 📐 Настройки хедера

| Параметр | Значения |
|----------|----------|
| **Вариант** | `default`, `centered`, `minimal`, `with-cta` |
| **Прилипающий** | вкл/выкл |
| **Бургер** | вкл/выкл |
| **Брейкпоинт** | `sm`, `md`, `lg`, `xl` |

**Варианты** **хедера:**
- **default** — логотип слева, меню, контакты справа.
- **centered** — логотип по центру, меню внизу.
- **minimal** — только логотип + бургер.
- **with-cta** — логотип, меню, контакты, кнопка «Заказать звонок».

**Добавить** **свой** — [TEMPLATES.md](../for-developers/TEMPLATES.md).

---

## 📐 Настройки футера

| Параметр | Значения |
|----------|----------|
| **Вариант** | `default`, `minimal`, `dark`, `light` |
| **Копирайт** | HTML разрешён |

**Варианты** **футера:**
- **default** — тёмный (`#1A1E23`), меню, контакты, соцсети.
- **dark** — тёмнее, акцентные иконки.
- **light** — светлый.
- **minimal** — логотип + копирайт.

---

## 🔍 SEO настройки

| Поле | Рекомендуемая длина |
|------|---------------------|
| **Meta Title** | 50-60 символов |
| **Meta Description** | 150-160 символов |
| **Meta Keywords** | через запятую |

**Meta Title** — если пусто, используется `{title страницы}`.  
**Meta Description** — приоритет у глобального.

---

## 📱 Open Graph & Twitter

| Поле | Назначение |
|------|-----------|
| **OG Title** | Заголовок для соцсетей |
| **OG Description** | Описание |
| **OG Image** | 1200×630px |
| **OG URL** | Канонический URL |

**Twitter Card** — то же, но для Twitter/X.

**Если** **пусто** — **берётся** **из** **SEO**/**Twitter**.

---

## 🔗 Социальные сети

**13 сетей** (выбор из дропдауна):
VK, OK, Telegram, Telegram-канал, WhatsApp, Instagram, Facebook, Twitter/X, YouTube, TikTok, GitHub, LinkedIn, RSS.

**3 основные** (VK, OK, Telegram) — **нельзя** **удалить**.

**Добавление:** дропдаун → выбор → ввод URL → «Добавить».

**Иконки** — из `/fonts/brands.svg`.

---

## 🗜 Минификация

| Параметр | Что делает |
|----------|-----------|
| **Включить минификацию** | Создаёт `.min.css`/`.min.js` для модулей |
| **Объединение** | Все CSS/JS страницы в один файл |

**Исключения:** кнопка **«Исключения»** → модалка с чекбоксами CSS/JS **по** **каждому** **модулю**.

**Статистика:** сколько **минифицировано**, **размер** **до**/**после**.

**Кнопки:**
- **Пересобрать** **все** `.min`.
- **Удалить** **все** `.min`.

**Когда** **включать:** **на** **проде** **для** **скорости**. **На** **разработке** — **выключено** (**легче** **отлаживать**).

---

## ⚡ Кеширование страниц

| Параметр | Что делает |
|----------|-----------|
| **Включить кеширование** | Готовый HTML в `/cache/pages/` |
| **Время жизни** | 1ч / 6ч / 24ч / 7д / бессрочно |

**Статистика:** **страниц** **в** **кеше**, **объём**.

**Кнопка** **«Очистить** **весь** **кеш»** — **удаляет** **всё**.

**Когда** **включать:** **на** **проде**. **На** **разработке** — **выключено** (**видеть** **изменения** **сразу**).

**Автоматическая** **очистка** **при:**
- **Сохранении** **страницы** — **только** **её** **кеш**.
- **Обновлении** **модуля** — **весь** **кеш** **ассетов**.
- **Изменении** **настроек** **ассетов** — **весь** **кеш**.

---

## 💻 Произвольный код

### Custom CSS

**Вставляется** **в** `<style>` **в** `<head>`.

**Пример:**
```css
.btn-custom {
    background: linear-gradient(90deg, #10B981, #059669);
    box-shadow: 0 4px 20px rgba(16, 185, 129, 0.3);
}
```

### Custom JS

**Вставляется** **в** `<script>` **перед** `</body>`.

**Пример:**
```js
document.addEventListener('DOMContentLoaded', function() {
    console.log('Custom JS loaded');
});
```

**Внимание:** **это** **доверенный** **код** — **санитайзер** **не** **применяется**. **Проверяй** **перед** **вставкой**.

---

## 💾 Сохранение

**Кнопка** **«Сохранить** **настройки»** **внизу** **страницы**.

**При** **сохранении:**
- **Данные** **пишутся** **в** `data/settings.json`.
- **Логирование** (`settings_save`).
- **Flash-сообщение** «Настройки сайта успешно сохранены!».
- **Очистка** **кеша** (**если** **изменились** **ассеты**).

**Ошибки:**
- **HTML** **в** **запрещённом** **поле** → **«Поле "X": Обнаружено недопустимое содержимое: ...»**.
- **Невалидный** **email** → **«Некорректный email»**.
- **Ошибка** **записи** → **«Ошибка записи на диск»**.

---

## 📞 Поддержка

- **Документация:** [docs/](../)
- **FAQ:** [FAQ.md](FAQ.md)

---

**Следующий шаг:** [BACKUPS.md](BACKUPS.md)